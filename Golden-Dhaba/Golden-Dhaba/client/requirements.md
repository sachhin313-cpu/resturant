## Packages
zustand | Global state management for the shopping cart
framer-motion | Page transitions and scroll-triggered animations

## Notes
- `api` and `buildUrl` are expected from `@shared/routes`.
- Schemas like `loginSchema`, `checkoutSchema`, `insertUserSchema`, `insertMenuItemSchema` are expected from `@shared/schema`.
- All styling uses a premium dark mode by default with golden accents to reflect the "Punjabi dhaba" theme.
